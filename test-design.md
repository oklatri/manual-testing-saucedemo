# Test Design — SauceDemo

## Что это
Техники тест-дизайна, применённые мной к SauceDemo.

## 1. Equivalence Partitioning (EP) — классы эквивалентности

**Идея:** разбить входные данные на группы (классы), которые ведут себя одинаково. Из каждого класса проверить одно значение.

### Пример: поле First Name (Checkout)

**Классы:**
- Валидные: буквы (`Ivan`, `Maria`).
- Невалидные: цифры (`111`, `123`).
- Невалидные: спецсимволы (`!`, `@`).
- Невалидные: пусто.

### Пример: поле Zip (Checkout)

**Классы:**
- Валидные: 5 цифр (`12345`).
- Невалидные: меньше 5 цифр (`123`).
- Невалидные: больше 5 цифр (`1234567890`).
- Невалидные: буквы (`abcde`).
- Невалидные: пусто.

## 2. Boundary Value Analysis (BVA) — граничные значения

**Идея:** баги чаще на границах диапазона. Проверяем границы и чуть за ними.

### Пример: поле Zip (принимает 5–10 цифр)

| # | Значение | Что проверяет | Ожидание |
|---|---|---|---|
| 1 | `1234` | Граница −1 (4 цифры) | Ошибка |
| 2 | `12345` | Нижняя граница (5 цифр) | Принято |
| 3 | `123456` | Граница +1 (6 цифр) | Принято |
| 4 | `123456789` | Середина (9 цифр) | Принято |
| 5 | `1234567890` | Верхняя граница (10 цифр) | Принято |
| 6 | `12345678901` | Граница +1 (11 цифр) | Ошибка |

## 3. Применение к SauceDemo

### Поле First Name
- **EP:** валидные (буквы), невалидные (цифры, символы, пусто).
- **BVA:** не применимо (нет диапазона).

### Поле Last Name
- **EP:** аналогично First Name.

### Поле Zip
- **EP:** валидные (5 цифр), невалидные (< 5, > 5, буквы, пусто).
- **BVA:** 4, 5, 6, 9, 10, 11 цифр.

## 4. Результаты применения

На основе этих техник созданы тест-кейсы **TC-004**, **TC-005**, **TC-014** (см. `test-cases.md`).

## 5. Зачем это нужно

- **EP** — сокращает количество тестов (проверяем 1 из класса, а не все).
- **BVA** — находит баги на границах (самое частое место).
- **Комбинация** — покрывает почти все возможные баги за минимум тестов.

## 6. Таблица принятия решений (Decision Table)

**Идея:** для сложных бизнес-правил, где разные комбинации условий дают разные результаты.

### Пример: скидки в интернет-магазине

| Постоянный клиент | Покупка > 100$ | Скидка |
|---|---|---|
| Да | Да | 10% |
| Да | Нет | 0% |
| Нет | Да | 5% |
| Нет | Нет | 0% |

### Применение к SauceDemo

Хотя в SauceDemo нет скидок, эта техника **применима** к **оформлению заказа**:

| Товар в корзине | Форма заполнена | Результат |
|---|---|---|
| Да | Да | Заказ создан |
| Да | Нет | Ошибка валидации |
| Нет | Да | Переход на форму (баг — BUG-005) |
| Нет | Нет | Переход на форму |

## 7. Диаграмма состояний (State Transition)

**Идея:** для систем с конечными состояниями и переходами между ними.

### Применение к SauceDemo: статусы заказа

**Состояния:**
- `new` — новый заказ.
- `paid` — оплачен.
- `shipped` — отправлен.

**Переходы:**
- `new` → `paid` (оплата).
- `paid` → `shipped` (отправка).

### Применение к SauceDemo: корзина

**Состояния:**
- Пустая.
- С товарами.

**Переходы:**
- Пустая → С товарами (Add to cart).
- С товарами → Пустая (Remove / Reset App State).

**Баг (BUG-003):** переход «С товарами → Пустая» **не** **обновляет** **кнопки**.

## 8. Предугадывание ошибок (Error Guessing)

**Идея:** тестировщик **на** **основе** **опыта** **предполагает**, **где** **могут** **быть** **баги**.

### Применение к SauceDemo

**Где** **искать** **баги**:
- **Границы** **полей** (Zip — 5 цифр).
- **Спецсимволы** **в** **именах** (`!`, `@`, цифры).
- **Пустые** **поля**.
- **Двойное** **нажатие** **кнопок**.
- **Reset** **App** **State** (**наш** **BUG-003**).
- **Медленная** **загрузка** (`performance_glitch_user`).

## 9. Декомпозиция

**Идея:** разбить **сложную** **функциональность** **на** **более** **мелкие** **части**.

### Применение к SauceDemo

**Оформление** **заказа** **декомпозировано** **на**:
- **Корзина** (проверка **товаров**).
- **Checkout**: **форма** **данных**.
- **Checkout**: **обзор**.
- **Checkout**: **завершение**.

**Каждый** **этап** — **отдельные** **тест-кейсы** (**TC-012**, **TC-013**, **TC-014**).

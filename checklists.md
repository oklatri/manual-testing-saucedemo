# Checklists — SauceDemo

## Логин

- ✅ `standard_user` — заходит
- ✅ `locked_out_user` — блокируется с ошибкой «Sorry, this user has been locked out»
- ⚠️ `problem_user` — заходит, но все фото товаров одинаковые (BUG-007)
- ⚠️ `performance_glitch_user` — заходит, но страницы грузятся медленно (BUG-009)
- ✅ `error_user` — заходит
- ⚠️ `visual_user` — заходит, но цены меняются при обновлении (BUG-010)

## Страница товаров

- ✅ Товары отображаются
- ✅ Цены отображаются
- ✅ Сортировка работает
- ✅ Фильтры работают

## Корзина

- ✅ Добавление товара работает
- ✅ Удаление товара работает
- ✅ Счётчик корзины обновляется
- ❌ Reset App State не сбрасывает кнопки товаров (BUG-003)
- ❌ Кнопки не обновляются при смене фильтров (BUG-003)

## Оформление заказа

- ✅ Переход к Checkout работает
- ❌ Форма принимает цифры и символы в имени (BUG-004)
- ❌ Можно оформить заказ с пустой корзиной (BUG-005)
- ❌ `error_user` не может ввести Last Name, но Continue активен (BUG-006)
- ❌ `problem_user` не может ввести Last Name (BUG-008)
- ❌ Кнопка Continue смещена у `visual_user` (BUG-012)

## Гамбургер-меню

- ✅ All Items работает
- ❌ Dynamic Catalog грузит бесконечно повторяющиеся товары (BUG-001)
- ❌ About возвращает 403 Forbidden (BUG-002)

## Визуальные проверки

- ❌ Значок корзины смещён у `visual_user` (BUG-011)
- ❌ Кнопка Continue смещена у `visual_user` (BUG-012)
